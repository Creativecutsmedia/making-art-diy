[build]
  command = "pip install -r requirements.txt && python3 update-products.py"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[redirects]]
  from = "/_products/*"
  to = "/"
  status = 404
  force = true

# TODO(fase 3a): add server-side gate for /admin-v2/* before real data lands.
# Currently relies on client-side Identity check only (Option B, Free tier).
# Options: (A) Netlify Pro + role-based redirect with Role=admin condition;
# (B) Netlify Edge Function validating nf_jwt cookie. Must be in place before
# wiring real orders, customers, or Stripe data into admin-v2.
